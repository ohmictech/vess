[package]
name = "vess-protocol"
version = "0.1.0"
edition = "2021"
license-file = "../LICENSE"

[dependencies]
serde = { version = "1", features = ["derive"] }
postcard = { version = "1", features = ["alloc"] }

[dev-dependencies]
blake3 = "1"
