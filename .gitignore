/target/
/vess-pool/
/vess-verify/target/

# Untracked helper scripts (local only)
vess-miner/examples/bridge_eth.rs

# Secrets — never commit
sepolia.key
*.key
miner.key
miner.json
miner.address
miner.toml
