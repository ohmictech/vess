/target/
/vess-pool/

# Untracked helper scripts (local only)
vess-miner/examples/bridge_eth.rs

# Secrets — never commit
sepolia.key
*.key
miner.key
miner.json
miner.address
miner.toml

# local Etherscan API key (Arbiscan verification)
.etherscan-key
