# Stylus workspace configuration (used by cargo-stylus check/deploy)
# cargo-stylus 0.10 reads network endpoints and the max fee flag from here / CLI.
[workspace.networks.sepolia]
endpoint = "https://sepolia-rollup.arbitrum.io/rpc"

[workspace.networks.mainnet]
endpoint = "https://arb1.arbitrum.io/rpc"
