[package]
name = "vess-contract"
version = "0.1.0"
edition = "2021"

[dependencies]
stylus-sdk = "0.10"
alloy-sol-types = "1"
vess-crypto = { path = "../vess-crypto", default-features = false, features = ["verify"] }

# `cargo stylus export-abi` needs this.
[features]
export-abi = ["stylus-sdk/export-abi"]

# Enables the SDK's TestVM mock so `cargo test` links and runs natively.
# Dev-only: never unified into wasm32 builds.
[dev-dependencies]
stylus-sdk = { version = "0.10", features = ["stylus-test"] }

# stylus-sdk macros emit `#[cfg(feature = ...)]` for these undeclared features;
# register the cfgs so `-D warnings` stays clean.
[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = [
    'cfg(feature, values("contract-client-gen", "stylus-test"))',
] }

[lib]
crate-type = ["cdylib"]
